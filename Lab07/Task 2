#include <stdio.h>
int main() {
    int a1[10];
    int i;
    printf("Enter 10 numbers:");
    for(i=0;i<10;i++) { 
        scanf("%d", &a1[i]);
        }

    int key, count = 0;
    printf("Enter number to search: ");
    scanf("%d", &key);

  for( i=0;i<10;i++) {
        if (a1[i] == key) 
        count++;
    }

    if (count == 0) printf("Number not found\n");
    else printf("%d occurred %d times\n", key, count);
    return 0;
}
